# 模拟炒股（猜股票）· 完整游戏规则 · **V2**

> 本文**逐条对齐代码**，不是产品文案。每个数字后面都标了落点（文件:行），
> 改规则时请同步改这里。行号对应当前工作区版本，**函数名是稳定锚点**（行号会漂移）。
> 最后核对：2026-09-20（V2：每日两阶段 + 双成交价 + 每日额度）。
>
> 代码落点速查：
> - 阶段类型定义 → `types/index.ts:447`（`SimTradeStage`）
> - 费率常量 → `lib/constants.ts:6-19`
> - 费用/数量/成本纯函数 → `lib/tradingRules.ts`
> - 撮合与校验 → `services/tradingEngine.ts:placeOrder`
> - 阶段状态机 / 玩法流程 → `services/simtradeService.ts`
> - 前端阶段 UI → `components/SimTradeClient.tsx`

---

## 一、一局怎么进行（V2：每个交易日拆成**两个独立交易阶段**）

V2 与 V1 最大的区别：**一天不再只有一次操作，而是「开盘阶段 + 收盘阶段」各一次**，
并且两个阶段的**成交价不同**（开盘阶段按开盘价、收盘阶段按收盘价）。

```
                 ┌─────────────────── 每一个交易日 ───────────────────┐

  OPEN ──提交操作（按【开盘价】成交）──► OPEN_CONFIRMED
  （开盘阶段）                                │
                                       点「查看今日收盘」
                                    （服务端在此揭示当日收盘价）
                                              ▼
                                       CLOSE_ANIMATION ──动画播完自动──► CLOSE
                                       （收盘价公布中）                  （收盘阶段）
                                                                            │
                                                                      提交操作
                                                                  （按【收盘价】成交）
                                                                            ▼
                                                                     CLOSE_CONFIRMED
                                                                            │
                                                                      点「结算今日」
                                                                     （重算当日资产快照）
                                                                            ▼
                                                                      DAY_SETTLED
                                                                            │
                                                                  点「进入下一交易日」
                                                                            ▼
                                                          下一交易日 OPEN
                                                     （末日 → FINISHED + 揭晓）
```

状态由**服务端** `session.stage` 唯一决定，客户端无法干预（`simtradeService.ts:1244`
`advanceSimTradeStage` 是唯一的阶段推进入口）。

| 阶段 | 含义 | 成交价 | 可否下单 |
|---|---|---|---|
| `OPEN` | 开盘阶段 | **当日开盘价** | ✅ |
| `OPEN_CONFIRMED` | 开盘阶段已完成 1 次操作 | — | ❌（只能「查看今日收盘」） |
| `CLOSE_ANIMATION` | 收盘价公布中（动画播放） | — | ❌ |
| `CLOSE` | 收盘阶段 | **当日收盘价** | ✅ |
| `CLOSE_CONFIRMED` | 收盘阶段已完成 1 次操作 | — | ❌（只能「结算今日」） |
| `DAY_SETTLED` | 当日已结算 | — | ❌（只能「进入下一交易日」） |

**硬约束**：

- **每阶段只能操作 1 次**：`stageActionCompleted = true` 后再提交会被拒
  （`simtradeService.ts:1010`），必须推进到下一阶段。
- **未操作不能推进**：处于 `OPEN` / `CLOSE` 且未操作时点推进会被拒（`stageHint`，`:124`）。
- **动作三选一**：观望 / 买入（加仓）/ 卖出。**观望同样结束本阶段**，但不动仓、不消耗额度。
- **交易失败不结束阶段**：资金不足 / 持仓不足 / T+1 被拒时，阶段**保持原状、额度不消耗**，
  玩家可以重新选择（`:1104-1110` 释放占位后原样返回失败）。

## 二、标的与局参数

| 项 | 规则 | 落点 |
|---|---|---|
| 标的 | 从**全市场真实个股**随机抽 1 只，**身份全程隐藏**，只在棋局结束后才可揭晓 | `pickRandomTarget`、`revealSimTrade` |
| 抽样方式 | `ORDER BY RANDOM()` 无放回抽 **60 只候选/批**，最多换 **4 批**（共 240 只） | `:89`、`:96`、`pickRandomTarget:399` |
| 为什么换批 | 模拟起始日是随机落在全历史里的，单批 60 只有小概率集体不覆盖该窗口；换批重试把「整局创建失败」压到可忽略 | `pickRandomTargetOnce:316` |
| 股票池 | **仅「全部 A 股」**；**排除 ST / \*ST / 退市整理期**（名称含 `ST`） | `marketDataService.ts:1042` |
| 合格条件 | 该股日 K 根数 ≥ **83 根**（= 60 根可见历史 + 最多 23 个交易日） | `:85`、`:87` |
| 可见历史 | 开局前至少有 **60 根**真实日 K 供你判断盘感 | `:85` |
| 局时长 | **20~23 个交易日**（默认 **22**），创建时固化真实交易日历 | `:82`、`:83` |
| 初始资金 | 默认 **¥100,000**（创建时可改，上限 10 亿） | `DEFAULT_INITIAL_CASH` |
| 复权口径 | 用该股自身的口径（前复权为主，部分标的不复权） | `toAdjust` |
| 只有一只票 | 单一标的、**不可分散、不可做空**（只能买与卖已有持仓） | — |

### 2.1 股票池：为什么只有「全部 A 股」

V2 需求里曾设想 INDEX（指数）/ INDUSTRY（行业板块）两个池，**当前架构下无法实现**，
因此服务端对这两个池**明确拒绝**（`createSimTradeSession`，`:480`），
**绝不退回造假数据或静默降级**：

| 池 | 现状 | 拒绝理由 |
|---|---|---|
| `STOCK` | ✅ 可用 | — |
| `INDEX` | ❌ 拒绝 | 指数在 `market_index` / `index_klines` **物理分表**，与 `stocks` 无外键关联；而 `tradingEngine` 的 `Position`/`Order`/`Trade` 全部以 `stockId` 外键落库，承载不了指数标的（这是刻意的物理隔离设计） |
| `INDUSTRY` | ❌ 拒绝 | 库中 `stocks.industry` **尚未导入（全为 null）**，没有真实板块数据 |

## 三、成交规则

### 3.1 成交价 = **按阶段决定**（V2 关键设计）

| 阶段 | 成交价 | 代码 |
|---|---|---|
| `OPEN` | **当日开盘价** `bar.open` | `simtradeService.ts:1030` |
| `CLOSE` | **当日收盘价** `bar.close` | 同上 |

- 开盘阶段：你只看得到今日开盘价（唯一提前揭示的当日价格），**就在开盘价成交**；
- 收盘阶段：当日收盘价已在 `CLOSE_ANIMATION` 阶段公布，**就在收盘价成交**；
- 实现方式：用 **LIMIT 单 + 阶段成交价**把价格精确下沉给 `tradingEngine.placeOrder`，
  引擎仍完整负责资金 / 持仓 / T+1 / 停牌校验与费用计算 —— 本文件**不复制任何交易规则**。
- 成交日必须**严格等于**当前模拟交易日；若该股当日**停牌**（无行情），直接拒绝成交，
  不会回落到旧价成交（`placeOrder`）。

### 3.2 数量规则

| 方向 | 规则 | 落点 |
|---|---|---|
| 买入 | 必须是 **100 股（一手）的整数倍** | `tradingRules.ts:validateQuantity` |
| 卖出 | 允许**零股**（正整数字即可）；**100% 时直接卖出全部可卖份额**，不因不足一手而卖不掉 | `resolveQuantity:1165` |

### 3.3 比例怎么折算（V2：滑块，不是档位按钮）

前端用**滑块 + 数字输入框双向同步**，范围 **1~100 的整数百分比**
（`RatioSlider`，`SimTradeClient.tsx:994`）；滑块只在**已选动作之后**出现，
避免「拖滑块隐式选中买入」。

- **买入**：`预算 = 可用现金 × 比例`，在「成交额 + 实际买入费用 ≤ 预算」约束下
  取**最大的整手数量**（`resolveQuantity`，`:1165`）。
  费用随成交额线性变化，所以是**逐手回退求最大可买量**，不预留整手成本 ——
  这样 100% 才真正贴近满仓，且大额预算下不会因低估费用而整单被拒。
- **卖出**：`可卖份额 × 比例` → 整手向下取整；**100% 时取全部可卖份额**。
- 折算出的数��若 ≤ 0：买入报「可用资金不足，无法按该比例买入」，卖出报「可卖持仓不足…」。
- **比例必须显式给出**：缺省 / `null` / `NaN` / 越界一律拒绝，
  **绝不静默退化为 100% 满仓**（`submitSimTradeAction`，`:1046`）。

### 3.4 每日额度（V2 新增）

- **每日成功买入 ≤ 2 次、成功卖出 ≤ 2 次**（`MAX_BUY_PER_DAY` / `MAX_SELL_PER_DAY`，`:99`、`:101`）；
- 额度由**开盘 + 收盘两个阶段共享**；跨日后自动重置（`advanceSimTradeStage` 第 ④ 步）；
- **观望不消耗额度**，但仍结束本阶段；
- **交易失败不消耗额度**，也不结束阶段；
- 页面顶部阶段状态条实时显示「今日可买 N 次 / 今日可卖 N 次」（`StageBanner`，`SimTradeClient.tsx:554`）。

> 说明：因为每天只有 2 个阶段、每阶段只能操作 1 次，「每日 ≤ 2 次」在正常流程下
> 与「开盘 1 次 + 收盘 1 次」**等价**。额度计数主要作用是显式护栏（阶段数未来若变化时
> 不会失控）与给玩家看剩余次数。

## 四、费用规则

三项费用，**全部按成交额比例计算**，逐笔四舍五入到分。费用规则**只在
`lib/tradingRules.ts` 定义一处**，其余模块（含结算对照）一律调用 `calcFees`，杜绝规则漂移：

| 费种 | 费率 | 方向 | 最低收取 | 常量 |
|---|---|---|---|---|
| **佣金** | 万分之三（0.03%） | **双向**（买也收、卖也收） | **¥5 起** | `COMMISSION_RATE=0.0003`、`COMMISSION_MIN=5` |
| **印花税** | 千分之一（0.1%） | **仅卖出** | 无 | `STAMP_TAX_RATE=0.001` |
| **过户费** | 万分之 0.1（0.001%） | **双向**、沪深两市都收 | 无 | `TRANSFER_FEE_RATE=0.00001` |

计算方式（`tradingRules.ts:calcFees`）：

```
成交额 = 成交价 × 数量           （四舍五入到分）
佣金   = max( round2(成交额 × 0.0003), 5 )
印花税 = 卖出 ? round2(成交额 × 0.001) : 0
过户费 = round2(成交额 × 0.00001)
费用合计 = 佣金 + 印花税 + 过户费
```

**买入实际支出 = 成交额 + 费用合计**（`calcBuyOutlay`）
**卖出实际到账 = 成交额 − 费用合计**（`calcSellNetIncome`）

**门槛提示**：佣金最低 5 元 ⇒ 成交额低于约 **¥16,667** 的委托，佣金一律按 5 元收（相当于费率被动抬高）。

### 4.1 费用算例（可直接核对）

**买入 13,500 股 @ ¥3.66**：

```
成交额 = 13500 × 3.66 = ¥49,410.00
佣金   = 49410 × 0.0003 = 14.823 → ¥14.82
印花税 = 买入不收                 → ¥0.00
过户费 = 49410 × 0.00001 = 0.4941 → ¥0.49
费用合计 = ¥15.31
实际支出 = 49,410.00 + 15.31 = ¥49,425.31
```

**同样金额的卖出（对比）**：

```
成交额 ¥49,410.00
佣金 ¥14.82 + 印花税 ¥49.41 + 过户费 ¥0.49 = 费用合计 ¥64.72
实际到账 = 49,410.00 − 64.72 = ¥49,345.28
```

> 一买一卖来回一趟的摩擦成本 ≈ **成交额的 0.16%**（千分之 1.6），
> 所以模拟里也别频繁来回，成本是真实的。

## 五、持仓成本与盈亏口径

| 项 | 口径 | 落点 |
|---|---|---|
| 成本均价 | `(原成本总额 + 本次实际支出) / 新数量`，保留 **6 位小数** | `tradingRules.ts:calcAvgCostAfterBuy` |
| 为什么 6 位 | 成本均价若只留 2 位，1000 股就会偏差最多 5 元，会破坏「累计盈亏 = 已实现盈亏」的资产恒等式 | `tradingRules.ts` |
| 卖出已实现盈亏 | `(卖价 − 成本均价) × 数量 − 卖出费用` | `tradingRules.ts:calcRealizedPnl` |
| 总资产 | `现金 + 持仓市值`（市值按当日收盘价） | `tradingEngine.getAccountSummary` |
| 资产快照 | 每次「确认操作」后按当日收盘重算；「结算今日」与「进入下一交易日」时再重算 | `refreshDailyAsset` |
| 每日资产曲线 | 逐日落库 `daily_assets`，用于收益/回撤/夏普等指标 | `refreshDailyAsset` |

## 六、T+1 与各类拒绝清单

- **T+1**：当日买入的份额**当日不可卖**；跨日时由 `settleT1` 解冻为可卖。
  页面上「持仓 N 股 / 可卖 M 股」两个数字的差额就是当日被冻结的部分。
- 会被**拒绝**的情形（全部来自代码，不是臆测）：

| 情形 | 提示 |
|---|---|
| 阶段不可交易（`OPEN_CONFIRMED` / `CLOSE_ANIMATION` / `CLOSE_CONFIRMED` / `DAY_SETTLED`） | `stageHint(stage)`，如「本阶段已完成操作，请推进到下一阶段」 |
| 本阶段已操作过还想再操作 | 「本阶段已完成操作，请推进到下一阶段」 |
| 今日买入次数已用完（≥2 次） | 「今日买入次数已用完（每日上限 2 次），本阶段请选择观望」 |
| 今日卖出次数已用完（≥2 次） | 「今日卖出次数已用完（每日上限 2 次），本阶段请选择观望」 |
| 可用现金 < 成交额 + 费用 | 「可用资金不足：需 ¥X，可用 ¥Y」（引擎事务内校验） |
| 未持有该股却要卖 | 未持有 X，无法卖出 |
| 卖出数量 > 可卖数量（T+1 冻结） | 可卖数量不足（T+1 限制） |
| 该股当日无行情（停牌/未上市） | 「当前交易日该标的无行情，无法操作」 |
| 比例缺省 / 非数字 / 不在 1~100 | 「操作比例必须是 1~100 之间的数字」/「操作比例必须在 1~100 之间」 |
| 局已结束 | 「该模拟炒股已结束，无法继续操作」 |
| 股票池选了 INDEX / INDUSTRY | 创建时即拒绝（见 §2.1） |

## 七、防未来数据泄露（V2：**按阶段分级揭示**）

模拟炒股比普通回测更严，以下全部是**服务端强制**、前端无法绕过：

1. **行情可见上界 = 会话的 `currentDate`**：取 K 线一律右端锁死在该日；
   下单时还把 `asOfDate = currentDate` 下沉给引擎，引擎二次拒绝更晚的成交日。
2. **当日那根 K 线的揭示程度随阶段变化**（`buildVisibleHistory:615`、`isCloseRevealed:119`）：

   | 阶段 | 当日 K 线内容 | `snapshot.todayClose` |
   |---|---|---|
   | `OPEN` | **只有开盘价**（high/low/close 用 open 占位） | `null` |
   | `OPEN_CONFIRMED` | 同上（**仍不揭示**） | `null` |
   | `CLOSE_ANIMATION` 及之后 | 完整 OHLC | 当日收盘价 |

   即：**收盘价只在 `CLOSE_ANIMATION` 阶段才由服务端揭示**，前端拿到前无从推算。
3. **标的身隐藏**：代码 / 名称不出现在任何出参里（有测试守着 `assertNoIdentity`），
   只有棋局结束才允许揭晓（`revealSimTrade`）。
4. **大盘参照数据**：`snapshot.benchmarks` 仍按同一条红线计算（`closeRevealed` 决定取开盘还是收盘），
   但 **V2 前端已不再渲染任何指数卡片**，该字段保留仅为 API 兼容。

## 八、并发与幂等（V2 新增）

| 机制 | 说明 | 落点 |
|---|---|---|
| 下单原子占位 | 校验通过后用「以当前阶段 + 未操作为条件」的**条件更新**做 compare-and-swap；SQLite 写操作串行化，因此并发双提交只有一个能拿到 `count=1`，另一个被拒 | `submitSimTradeAction:1083-1094` |
| 交易失败释放占位 | 下单被引擎拒绝时把占位还原（阶段不变、额度不消耗） | `:1104-1110` |
| 阶段推进条件更新 | 4 个阶段流转全部用条件更新（`where: { stage: 期望源阶段 }`），双击不会跳过阶段 | `advanceSimTradeStage` |
| 阶段已变化 | 条件更新未命中说明**另一个请求已推进**，此时重新读快照按**幂等成功**返回（`staleStageResult`），不显示假报错 | `staleStageResult` |

> 为什么需要：V1 的「先读 `confirmedToday` 再写」是读后判断，双击 / 网络重试可让
> 两次请求同时通过校验、成交两笔。V2 用条件更新把这一步变成原子的。

## 九、结算与对照

- 每局结束后给出：初始资金、最终资产、总盈亏、收益率、交易次数、最大仓位、最大回撤；
- 并给**买入持有对照**：同区间、同费用口径下「一直拿着这只票」会怎样 —— 用来判断你的择时是否真有价值
  （对照口径同样调用 `calcFees`，与玩家交易完全一致）；
- 揭晓后才显示标的身份。

## 十、本玩法**没有**实现的规则（别误以为是 bug）

| 现实中的规则 | 本模拟 |
|---|---|
| 涨跌停限制 | **不校验**（停牌会拒绝，但没有 ±10%/±20% 限制） |
| 滑点 / 盘口撮合 | **无滑点模型**，一律按阶段成交价成交 |
| 限价单挂单等待 | 引擎支持 LIMIT，本玩法用 LIMIT **仅为了指定阶段成交价**，不做真实挂单排队 |
| 做空 / 融资 | **不支持**，只能买多 |
| 多标的分散 | **不支持**，一局只有一只隐藏标的 |
| 指数 / 行业板块池 | **不支持**（见 §2.1，服务端明确拒绝） |
| 分红送股 / 除权 | 用已复权（前复权为主）的日 K，**不单独模拟分红事件** |
| 最小 5 元佣金的"免佣券商" | 一律按万三、最低 5 元收，**没有免 5 政策** |

## 十一、一句话总结

**一只隐藏的真实 A 股、20~23 个交易日、10 万本金；每个交易日拆成「开盘阶段」和
「收盘阶段」，每阶段只能操作 1 次 —— 开盘阶段按当日开盘价成交，收盘阶段按当日收盘价成交；
每日最多买 2 次、卖 2 次；佣金万三（最低 5 元，双边）、印花税千一（只卖出）、过户费万 0.1（双边）；
T+1；收盘价只在收盘动画阶段才公布，全程看不到未来行情，也看不到自己买的是什么票 ——
最后跟"一直拿着"比收益。**

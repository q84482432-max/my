# 阶段5 校验与缺陷报告（30 分钟 K 线）

> 生成时间：2026-09-22
> 数据：`D:\AStockData\minutes_30\*.parquet`（5558 只）· 基准：`prisma/dev.db`（日K，2385505 行）
> 全部检查**只读**，未修改任何正式数据。

---

## 一、阶段5 补齐结果（写入任务）

| 项 | 值 |
|---|---|
| 计划补齐 | **814** 只 |
| 成功补齐到 2026-09-21 | **813** 只 |
| 失败 | **1** 只（`920792`） |
| 耗时 | 292 秒（ok=813 / fail=1 / skip=0） |

`920792` 失败原因：**新浪源端在 2026-09-21 缺 `11:00` 这一根**（返回 7 根而非 8 根）。下载器判定「每日根数 != 8」后**正确拒写**。
连续重试 **7 轮稳定复现同一缺口** → 判定为**合法源端缺口**，已如实记录，**未伪造补齐**。

---

## 二、coverage —— 逐文件交易日集合一致性

| 项 | 值 |
|---|---|
| parquet (code,date) 对 | 2385410 |
| dev.db 日K (code,date) 对 | 2385505 |
| parquet 标的数 | 5558 |
| 集合完全一致 | **5510** |
| 集合不一致 | 48 |
| 缺日（db 有 pq 无） | **95** |
| 多日（pq 有 db 无） | **0** |

95 个缺失日中：
- **94 个是历史既有缺口**（分布在 2024-11 ~ 2026-08，47 只，每只 1~7 天）——非本次引入
- **1 个是 `920792` 的 2026-09-21**（上节已定性为源端缺口）

> 结论：本次补齐**未引入任何新的覆盖缺口**，「多日 = 0」也排除了越界写入。

---

## 三、integrity —— 结构完整性（全市场单遍流式聚合）

扫描 5558 只 / **19,083,280** 根 bar：

| 判据 | 违规 bar | 违规标的 |
|---|---|---|
| 时点非标准 | 0 | 0 |
| 价格 ≤ 0 | 0 | 0 |
| OHLC 不变量 | 0 | 0 |
| amount != close × volume | 0 | 0 |
| 空日期/时点 | 0 | 0 |
| 每日根数 != 8 | — | 0 |

**结构不合格标的合计：0。**

---

## 四、越界 —— 30m 日聚合 vs dev.db 日K 边界

判据：每日 8 根聚合出的 O/H/L/C 必须落在 dev.db 当日日K `[low, high]` 内。

| 档位 | (股票,日期) 对 | 占比 |
|---|---|---|
| 参与比对 | 2,385,410 | 100% |
| 任一越界（ex > 0） | 58,493 | 2.45% |
| > 0.05% | 7,477 | 0.3134% |
| > 0.5% | 205 | 0.0086% |
| > 2% | **37** | 0.0016% |
| > 10% | **30** | 0.0013% |

> 「ex > 0」这一档绝大部分是**浮点/舍入噪声**（量级 < 0.05%），不代表缺陷。

---

## 五、越界异常定性（本轮核心结论）

37 对 > 2% 的异常，经逐对取证后**分成两个完全不同的类别**：

### 5.1 硬缺陷：北交所 24 只 @ 2026-06-30（27 根 bar）—— 新浪源端污染

**判据**：单根 bar 的 `high > 日K high × 1.5` 或 `low < 日K low / 1.5`（物理不可能）。

| 项 | 值 |
|---|---|
| 硬缺陷 bar | **27** 根 |
| 涉及 (股票,日期) | **24** |
| 涉及日期 | **1 天（2026-06-30）** |
| 板块 | **全部北交所（920xxx）** |
| 时点 | `14:00:00` × 24 根，`13:30:00` × 3 根 |

**根因链（已取证）**：

1. 直接读**新浪原始响应**（下载器主源，`money.finance.sina.com.cn/.../getKLineData?scale=30`）——
   `920001` 在 `2026-06-30 14:00` 的 `high` **就是 `26681.471`**，与本地 parquet **逐字节一致**。
   → 本地 parquet **忠实记录了源端数据，未引入任何污染**。
2. 污染**贯穿新浪全部分钟粒度**：`scale=5 / 15 / 30 / 60` 均含同一垃圾值
   （`920001` 在 15m 的 `13:45`、`14:00` 都是 `26681.471`；`920090` 在 5m/15m/30m/60m 都是 `30.000`）。
3. 复权因子**不可能**产生该值：因子是全天统一倍数（实测 `300176` 因子均匀 0.8867 / 0.8738），
   无法把 `16.10` 变成 `26681.471`。
4. 下载器的 `apply_qfq` 在缩放后做 `high = max(O,H,L,C)` / `low = min(...)` 兜底，
   **只能保证 OHLC 不变量成立，无法识别源头垃圾** —— 这正是 integrity 检查 0 违规、
   只有跨源边界比对才能抓到它的原因。

**污染样例**（`bar.high` 或 `bar.low` 被替换为其它数值）：

| code | date | time | bar O/H/L/C | 日K H/L | 偏离 | 类型 |
|---|---|---|---|---|---|---|
| `920001` | 2026-06-30 | 14:00 | 14.980 / **26681.471** / 14.830 / 14.910 | 16.10 / 14.10 | **1657×** | 上界 |
| `920023` | 2026-06-30 | 14:00 | 1.600 / **1001.020** / 1.590 / 1.600 | 1.66 / 1.58 | **603×** | 上界 |
| `920018` | 2026-06-30 | 14:00 | 14.770 / **3030.000** / 14.750 / 14.760 | 15.15 / 14.55 | **200×** | 上界 |
| `920005` | 2026-06-30 | 14:00 | 26.610 / **4549.050** / 26.560 / **4508.870** | 27.07 / 26.51 | **168×** | 上界+**close 也坏** |
| `920090` | 2026-06-30 | 13:30 & 14:00 | 2.800 / **30.000** / 2.790 / 2.800 | 2.87 / 2.77 | **10.5×** | 上界（连续 2 根） |
| `920204` | 2026-06-30 | 13:30 & 14:00 | 12.850 / 12.870 / **1.300** / 12.820 | 13.04 / 12.61 | **9.7×** | 下界（连续 2 根） |
| `920211` | 2026-06-30 | 14:00 | 128.950 / 129.000 / **14.530** / 128.750 | 129.94 / 122.85 | **8.5×** | 下界 |
| `920220` | 2026-06-30 | 14:00 | 53.750 / 53.770 / **14.530** / 53.490 | 54.30 / 52.00 | **3.6×** | 下界 |

> 完整 27 条清单见机器可读文件 `D:\AStockData\logs\p5_defect_manifest.json`。

**修复源穷举结果（均已实测）**：

| 源 | 结果 |
|---|---|
| 新浪自身（5m/15m/30m/60m） | ❌ 全部粒度含同一垃圾值 |
| 腾讯 `mkline?param=bj920001,m30,,320` | ❌ 北交所返回 `m30: []`（不提供该品种分钟线） |
| 腾讯 `newfqkline` | ❌ 只支持日K（`qfqday`），无分钟粒度 |
| 东财 `push2his.eastmoney.com` | ❌ 直连与走本机代理均不可达（连深市股票都 `RemoteDisconnected`） |
| 网易 | ❌ 域名 DNS 不可解析 |

> **结论：当前无可用替代源，这 27 根 bar 无法从外部恢复。**

### 5.2 软差异：`300176` 3 天（可接受）

| date | 新浪原始 H/L | 本地 parquet H/L | dev.db 日K H/L | 越界 |
|---|---|---|---|---|
| 2025-04-07 | 6.58 / 5.90 | 5.8344 / 4.9123 | 5.68 / 4.92 | 2.72% |
| 2025-04-09 | 5.2004(≈) | 5.2004 / 4.4701 | 5.19 / 4.58 | 2.40% |
| 2026-05-29 | 7.16 / 5.92 | 6.2563 / 5.1728 | 6.11 / 5.21 | 2.39% |

- 复权因子**完全均匀**（`300176` 2025-04-07：parquet/raw = 0.8867 对 O/H/L/C 四个字段一致）
  → 30m 数据**内部自洽**，无污染。
- 差异集中在 `10:00` 那根（当日首根）的 `open`/`high`，量级 2.4~2.7%。
- 定性为**跨源 OHLC 口径差异**（新浪 30m vs dev.db 日K 来源对当日极值的记录不同），
  非缺陷，**不处理**。

---

## 六、结论与待决事项

### 已确认无问题
- 结构完整性：**19,083,280 根 bar，0 违规**
- 覆盖一致性：**5510/5558 完全一致，多日 = 0**
- 阶段5 补齐：**813/814**，唯一失败为合法源端缺口

### 待决：北交所 24 只 @ 2026-06-30 的 27 根污染 bar 如何处置

**污染字段实测分布**：`high` 17 条、`low` 10 条、`close` 1 条（`920005`，与 `high` 同时坏）、`open` **0 条**。

#### 推荐方案：**同 bar 有界收敛**（不使用任何外部数据）

规则（只用**同一根 bar 自身的干净字段**，按序执行 —— 顺序关键）：

```
1) close 污染  →  close ← open
2) open  污染  →  open  ← close
3) high  污染  →  high  ← max(open, close, low)
4) low   污染  →  low   ← min(open, close, high)
5) 重保 OHLC 不变量，重算 amount ← close × volume
触发条件：仅当 越界倍数 > 1.5×（硬缺陷判据）时才动，避免误伤正常跨源差异
```

**演算结果（`_p5_repair_plan.py`，建议稿 `D:\AStockData\logs\p5_repair_proposal.json`）**：

| 指标 | 值 |
|---|---|
| 修复后仍落在 dev.db 当日日K `[low, high]` 内 | **27 / 27** ✅ |
| 需要跨源（dev.db）注入的字段数 | **0** |
| 受影响 bar 数 | 27（其余 2,385,383 对**一律不动**） |

样例：

| code | time | 污染字段 | 修复前 O/H/L/C | 修复后 O/H/L/C | 日K H/L |
|---|---|---|---|---|---|
| `920001` | 14:00 | high | 14.98 / **26681.471** / 14.83 / 14.91 | 14.98 / **14.98** / 14.83 / 14.91 | 16.10 / 14.10 |
| `920090` | 13:30 & 14:00 | high | 2.80 / **30.00** / 2.79 / 2.80 | 2.80 / **2.80** / 2.79 / 2.80 | 2.87 / 2.77 |
| `920204` | 13:30 & 14:00 | low | 12.85 / 12.87 / **1.30** / 12.82 | 12.85 / 12.87 / **12.82** / 12.82 | 13.04 / 12.61 |
| `920005` | 14:00 | high+close | 26.61 / **4549.05** / 26.56 / **4508.87** | 26.61 / **26.61** / 26.56 / **26.61** | 27.07 / 26.51 |

#### 为什么推荐它（而非隔离 / 整日剔除）

| 对比项 | 同 bar 收敛 | A 隔离标记 | C 整日剔除 |
|---|---|---|---|
| 是否注入外部数据 | **否**（只用本 bar 自身字段） | 否 | 否 |
| 下游是否需各自实现排除逻辑 | **否**，修一次全局正确 | 是，App/回测/指标都要读清单 | 否 |
| 数据保留 | 27 根被收敛，**168 根好数据保留** | 全保留但 24 只当日不可用 | 丢 192 根（保留 0 根） |
| 可审计 | **原值 + 新值 + 规则写入旁路日志**，可回滚 | 天然可审计 | 可审计 |
| 精确度 | 是**有界估计**而非原值 | 不涉及 | 不涉及 |

推理要点：`high`/`low` 是**派生量**，其真值已被源端销毁，任何恢复都是估计；但「落在物理可能区间内」是**可证明达成**的（27/27 已验证）。
既然不可能还原真值，那么目标应是「消除物理不可能值 + 零外部注入 + 全程可审计」，本方案同时满足三项。

#### 落地要点（修两处，不只修一次）

1. **一次性脚本**：对现有 24 只的 27 根按上述规则收敛（**先备份这 24 个 parquet**）
2. **把同一约束加进 `scripts/fetch_30m_kline.py` 的 `apply_qfq` 之后**，使未来任何**全量重抓自动收敛（幂等）**，
   否则一次全量重抓会把污染值重新写回 —— 这是「修在源头一次」优于「每个消费者修一遍」的关键
3. 旁路日志 `D:\AStockData\metadata\kline_30m_repairs.jsonl`：每行记 `code / date / time / field / 原值 / 新值 / 规则版本`。
   不写入 parquet，保持 8 列 schema 不变。

> 影响面：27 根 / 19,083,280 根 = **0.00014%**；股票 24 / 5558 = 0.43%；日期 1 天。

---

## 七、修复执行结果（2026-09-22 20:41 完成）

### 执行方式

| 步骤 | 内容 |
|---|---|
| 1 | `scripts/repair_30m_defects.py` 首轮：阈值 1.5×，收敛 **27 根 / 24 只**（备份 `minutes_30_pre_repair_20260922-203315`） |
| 2 | 范围复查发现事故真实范围是 **34 对**（残留 10 对，越界 3.40%~45.72%，全在 2026-06-30 14:00） |
| 3 | 二轮：按**事故成员**界定（`--ratio 1.0`），收敛剩余 **10 根 / 10 只**（备份 `…-204000-incident`） |
| 4 | 下载器 `apply_qfq` 加 `clamp_to_dayk` 兜底 → 全量重抓自动收敛（幂等） |

审计日志：`D:\AStockData\metadata\kline_30m_repairs.jsonl`（累计 **37 行**，含 code/date/time/字段/原值/新值/规则版本）

### 效果对照

| 指标 | 修复前 | 修复后 | 变化 |
|---|---|---|---|
| **硬缺陷 bar**（>1.5× 越界） | 27 | **0** | **−27** ✅ |
| 2026-06-30 北交所越界 bar | 34 | **0** | **−34** ✅ |
| 越界 >10% 对 | 30 | **0** | **−30** ✅ |
| 越界 >2% 对 | 37 | **3** | −34 |
| 越界 >0.5% 对 | 205 | **171** | −34 |
| **max 越界幅度** | **165,623.42%** | **2.7183%** | ✅ 回到正常噪声区间 |
| coverage（一致 / 缺 / 多） | 5510 / 95 / 0 | **5510 / 95 / 0** | 未变 ✅ |
| integrity（19,083,280 根） | 0 违规 | **0 违规** | 未变 ✅ |

- 每个档位恰好减少 **34**（= 24 + 10 只 × 1 天），证明改动是**外科式**的：其余 2,385,376 对一律未动。
- 残留 **3 对 >2%** 全部是 `300176`（3 个不同日期，2.39%~2.72%），即 5.2 节定性的**跨源口径差异**，**按设计保留不处理**。
- 残留 **171 对 >0.5%** 为同类跨源噪声（max 2.7183%），**均未触碰**。

### 已证伪的判据（记录以免后人重蹈）

我曾假设「单根 bar 内包络 `high/low > 1.30` 即垃圾值」可用于判定。**实测证伪**：
全市场 19,083,280 根中有 **141 根**满足该条件，但绝大多数是**新股上市首日**（无涨跌幅限制），
例如 `001399` 2026-06-26 10:00 `O=31.60 → H=76.00 → C=59.88`（`H/L=2.413`），属**合法**数据。

> 结论：**唯一可靠的判定是跨源比对**（30m 日聚合 vs dev.db 日K），
> 单靠 bar 自身的数值特征无法区分「上市首日巨幅波动」与「源端污染」。

# 复制为 .env 后按需修改（.env 已被 .gitignore 排除，不入库）
#   cp .env.example .env

# SQLite 数据库文件（相对 prisma/ 目录解析）—— 唯一必需项
DATABASE_URL="file:./dev.db"

# 真实 A股日K 数据源目录（只读引用）
# 仅导入脚本使用；数据库已含全量数据时可以不配
MARKET_SOURCE_DIR=""
